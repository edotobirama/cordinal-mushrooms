import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cordinal.mushrooms',
  appName: 'Cordinal Mushrooms',
  webDir: 'out'
};

export default config;
